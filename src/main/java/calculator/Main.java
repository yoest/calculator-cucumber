package calculator;

/**
 * only used to start the GUI from the jar
 * Without this class, the jar doesn't work (an exception is thrown)
 */
public class Main {
    public static void main(String[] args) {
        GuiStarter.main(args);
    }
}
